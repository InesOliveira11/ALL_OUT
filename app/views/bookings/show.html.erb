<div class="container">
  <div class="row mt-4 justify-content-center">
    <div class="col-6">
      <div class="card-trip">
        <div class="card-trip-infos">
          <div>
            <h2><%= @booking.activity.title %></h2>
            <p><%= @booking.activity.description %></p>
            <p><%= @booking.activity.price %></p>
            <p><%= @booking.activity.rating %></p>
            <p><%= @booking.activity.location %></p>
          </div>
          <h2 class="card-trip-pricing"><%= @booking.activity.price %></h2>
        </div>
      </div>
    </div>
  </div>
</div>
